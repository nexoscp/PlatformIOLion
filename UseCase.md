Create new Project (in CLion, IDEA)

Open existing PlatformIO Project first time (make sure no more state than platform.ini and output from command platformio is needed)

Import from VCS (detect by ini file)
